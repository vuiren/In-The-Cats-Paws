namespace Game_Project.Scripts.ViewLayer.Controllers.CatBotControllers
{
    public class CatBombUIController
    {
        /*private readonly IMyLogger _logger;
        
        public CatBombUIController(IMyLogger logger, IUnitsSelectionService selectionService)
        {
            _logger = logger;
            selectionService.RegisterOnUnitSelection(TryToShowUI);
            selectionService.RegisterOnUnitDeselection(TryToHideUI);
        }

        private void TryToHideUI(Unit obj)
        {
            if (obj.UnitType == UnitType.CatBotBomb)
            {
                HideCatBombUI(obj.GameObjectLink().GetComponent<CatBomb>());
            }
        }

        private void TryToShowUI(Unit obj)
        {
            if (obj.UnitType == UnitType.CatBotBomb)
            {
                ShowCatBombUI(obj.GameObjectLink().GetComponent<CatBomb>());
            }
        }

        private void ShowCatBombUI(CatBomb catBomb)
        {
            _logger.Log( $"Showing {catBomb.name} ui");
            catBomb.ShowUI();
            _logger.Log( $"Done showing {catBomb.name} ui");
        }

        private void HideCatBombUI(CatBomb catBomb)
        {
            _logger.Log( $"Hiding {catBomb.name} ui");
            catBomb.HideUI();
            _logger.Log( $"Done hiding {catBomb.name} ui");
        }*/
    }
}